name: CD

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  cd:
    runs-on: ubuntu-latest
    environment: test
    steps:
      - uses: peter-evans/repository-dispatch@v1
        with:
          token: ${{ secrets.DISPATCH_PAT }}
          event-type: hitl
          repository: quartiq/gha-runner-test
          client-payload: '{"ref": "${{ github.ref }}", "sha": "${{ github.sha }}"}'
